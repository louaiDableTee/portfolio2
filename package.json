{
  "name": "louai-bouraoui-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build && node scripts/check-build.mjs && node scripts/check-seo.mjs && node scripts/check-case.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^8.2.1"
  }
}
