{
  "name": "louai-bouraoui-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "capture": "node scripts/capture-worku.mjs",
    "check": "npm run build && node scripts/check-build.mjs && node scripts/check-seo.mjs && node scripts/check-case.mjs",
    "optimise": "node scripts/optimise-assets.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "sharp": "^0.35.4",
    "typescript": "^5.0.0",
    "vite": "^8.2.1"
  }
}
